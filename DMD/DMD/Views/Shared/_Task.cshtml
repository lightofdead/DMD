@model DMD.Models.Task

            <td>
                @Html.DisplayFor(modelItem => @Model.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => @Model.ParentTask.ID)
            </td>
            <td>
                <div class="btn-group">
                <a asp-action="Edit" asp-route-id="@Model.ID" class="btn btn-sm btn-info"> Edit </a> 
                <a asp-action="Details" asp-route-id="@Model.ID" class="btn btn-sm btn-primary ml-1"> Details </a>
                <a asp-action="Delete" asp-route-id="@Model.ID" class="btn btn-sm btn-danger ml-1"> Delete </a>
                </div>
            </td>
